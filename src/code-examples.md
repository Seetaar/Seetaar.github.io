# Примеры использования плагина Code Include

Плагин **mkdocs-codeinclude-plugin** позволяет включать код из внешних файлов прямо в документацию.

# Демонстрация включения кода

Эта страница демонстрирует различные способы включения кода из внешних файлов с помощью пользовательских макросов.

## Пример 1: Включение всего файла

Включение всего Python-файла целиком:

{{ code_block("examples/calculator.py") }}

---

## Пример 2: Включение по метке (add-method)

Включение только метода `add` с использованием метки `add-method`:

{{ code_block("examples/calculator.py", "add-method") }}

---

## Пример 3: Включение по метке (divide-method)

Включение только метода `divide` с обработкой ошибки деления на ноль:

{{ code_block("examples/calculator.py", "divide-method") }}

---

## Пример 4: Включение вспомогательной функции по метке

Включение вспомогательной функции `format_result`:

{{ code_block("examples/calculator.py", "helper-function") }}

---

## Пример 5: Включение конкретных строк (строки 1-10)

Включение только строк с 1 по 10 (документация модуля и импорты):

{{ code_from_file("examples/calculator.py", 1, 10) }}

---

## Пример 6: Включение конкретных строк (строки 80-95)

Включение метода `_save_to_history` (строки 80-95):

{{ code_from_file("examples/calculator.py", 80, 95) }}

---

## Пример 7: Включение примера использования

Включение демонстрационного блока кода из секции `__main__`:

{{ code_block("examples/calculator.py", "usage-example") }}

---

